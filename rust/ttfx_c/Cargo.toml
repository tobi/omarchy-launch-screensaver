[package]
name = "ttfx_c"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "staticlib"]
name = "ttfx_c"

[dependencies]
ttfx = { git = "https://github.com/omacom-io/ttfx" }
clap = { version = "4", features = ["derive"] }
